diff --git a/dev/assets/js/warzone-hotspots.js b/dev/assets/js/warzone-hotspots.js
index 5b01fe2..546d2ea 100644
--- a/dev/assets/js/warzone-hotspots.js
+++ b/dev/assets/js/warzone-hotspots.js
@@ -592,6 +592,18 @@ const STACK_OFF = [
     { x: -18, y: -14 },
     { x: 18, y: 14 },
 ];
+const HOTSPOT_LABEL_OFFSET = { x: 86, y: -52 };
+const HOTSPOT_RADIUS_OFFSET = { x: 0, y: 0 };
+function createHotspotRadiusEl(cluster) {
+    const el = document.createElement("div");
+    el.className = [
+        "wzhs-radius",
+        `wzhs-radius--${cluster.cat}`,
+        `wzhs-radius--sev-${cluster.sev}`,
+    ].filter(Boolean).join(" ");
+    el.setAttribute("aria-hidden", "true");
+    return el;
+}
 function stackVisible(clusters, overlapPx, maxPer) {
     const stacks = [];
     for (const c of clusters) {
@@ -619,19 +631,15 @@ function stackVisible(clusters, overlapPx, maxPer) {
     return out;
 }
 // â”€â”€â”€ DOM builders â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
-function buildExpandedHTML(items) {
-    return dedupeDisplayItems(items).slice(0, 6).map((e) => {
-        const sev = String(e.severity || "medium").toLowerCase();
+function buildExpandedHTML(cluster) {
+    const items = dedupeDisplayItems(cluster?.items || []);
+    if (!items.length) {
+        return `<div class="wzhs-item wzhs-item--headline"><strong class="wzhs-item__title">No active headlines available</strong></div>`;
+    }
+    return items.map((e) => {
         const title = e.__displayTitle || eventHeadline(e);
-        const subline = e.__displaySubline || eventSubline(e);
-        const time = timeAgo(e.occurred_at);
-        return `<div class="wzhs-item">
-            <div class="wzhs-item__row">
-                <span class="wzhs-item__sev wzhs-item__sev--${sev}">${sev.toUpperCase()}</span>
-                <span class="wzhs-item__time">${time}</span>
-            </div>
+        return `<div class="wzhs-item wzhs-item--headline">
             <strong class="wzhs-item__title">${title}</strong>
-            ${subline ? `<span class="wzhs-item__loc">${subline}</span>` : ""}
         </div>`;
     }).join("");
 }
@@ -678,7 +686,7 @@ function createCardEl(cluster, onToggle) {
                         ${loc ? `<span class="wzhs__loc">${loc}</span>` : ""}
                         <span class="wzhs__time">${time}</span>
                     </div>
-                    <div class="wzhs__items">${buildExpandedHTML(cluster.items)}</div>
+                    <div class="wzhs__items">${buildExpandedHTML(cluster)}</div>
                 </div>` : ""}
             </div>`;
     }
@@ -713,7 +721,7 @@ export function createWarzoneHotspotLayer(viewer, rootEl, options = {}) {
         clusterDistanceLon: options.clusterDistanceLon ?? 3.2,
         stackDistancePx: options.stackDistancePx ?? 100,
         maxVisiblePerHotspot: options.maxVisiblePerHotspot ?? 4,
-        minItemsForCluster: options.minItemsForCluster ?? 1,
+        minItemsForCluster: options.minItemsForCluster ?? 2,
         throttleIdle: options.throttleIdle ?? 100,
         throttleMove: options.throttleMove ?? 90,
     };
@@ -774,16 +782,23 @@ export function createWarzoneHotspotLayer(viewer, rootEl, options = {}) {
         for (const [id, node] of nodeMap) {
             if (!visibleIds.has(id)) {
                 node.el.remove();
+                node.radiusEl?.remove?.();
                 nodeMap.delete(id);
             }
         }
         for (const cluster of visible) {
             const off = STACK_OFF[cluster.stackIdx] || STACK_OFF[0];
-            const tx = cluster.screen.x + off.x;
-            const ty = cluster.screen.y + off.y;
+            const tx = cluster.screen.x + HOTSPOT_LABEL_OFFSET.x + off.x;
+            const ty = cluster.screen.y + HOTSPOT_LABEL_OFFSET.y + off.y;
+            const rx = cluster.screen.x + HOTSPOT_RADIUS_OFFSET.x;
+            const ry = cluster.screen.y + HOTSPOT_RADIUS_OFFSET.y;
             const zi = 25 - cluster.stackIdx;
             if (nodeMap.has(cluster.id)) {
                 const node = nodeMap.get(cluster.id);
+                if (!node.radiusEl) {
+                    node.radiusEl = createHotspotRadiusEl(cluster);
+                    rootEl.appendChild(node.radiusEl);
+                }
                 if (node.x !== tx || node.y !== ty) {
                     node.el.style.left = `${tx}px`;
                     node.el.style.top = `${ty}px`;
@@ -791,13 +806,23 @@ export function createWarzoneHotspotLayer(viewer, rootEl, options = {}) {
                     node.x = tx;
                     node.y = ty;
                 }
+                if (node.rx !== rx || node.ry !== ry) {
+                    node.radiusEl.style.left = `${rx}px`;
+                    node.radiusEl.style.top = `${ry}px`;
+                    node.rx = rx;
+                    node.ry = ry;
+                }
+                node.radiusEl.style.zIndex = zi - 1;
                 node.el.classList.toggle("wzhs--s2", cluster.stackIdx === 1);
                 node.el.classList.toggle("wzhs--s3", cluster.stackIdx === 2);
             } else {
+                const radiusEl = createHotspotRadiusEl(cluster);
+                radiusEl.style.cssText = `position:absolute;left:${rx}px;top:${ry}px;z-index:${zi - 1};`;
+                rootEl.appendChild(radiusEl);
                 const el = createCardEl(cluster, handleToggle);
                 el.style.cssText = `position:absolute;left:${tx}px;top:${ty}px;z-index:${zi};`;
                 rootEl.appendChild(el);
-                nodeMap.set(cluster.id, { el, x: tx, y: ty });
+                nodeMap.set(cluster.id, { el, radiusEl, x: tx, y: ty, rx, ry });
             }
         }
     }
@@ -876,13 +901,19 @@ export function createWarzoneHotspotLayer(viewer, rootEl, options = {}) {
             scheduleRender(0);
         },
         clear() {
-            for (const [, node] of nodeMap) node.el.remove();
+            for (const [, node] of nodeMap) {
+                node.el.remove();
+                node.radiusEl?.remove?.();
+            }
             nodeMap.clear();
         },
         destroy() {
             destroyed = true;
             clearTimeout(moveEndTimer);
-            for (const [, node] of nodeMap) node.el.remove();
+            for (const [, node] of nodeMap) {
+                node.el.remove();
+                node.radiusEl?.remove?.();
+            }
             nodeMap.clear();
             viewer.scene.postRender.removeEventListener(onPostRender);
             viewer.camera.moveStart.removeEventListener(onCameraMoveStart);
